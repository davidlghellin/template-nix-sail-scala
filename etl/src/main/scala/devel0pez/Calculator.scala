package devel0pez

/** A pure function with no Spark in sight: it proves the project compiles and the suite runs
  * without paying for a session.
  */
object Calculator {

  /** Adds two numbers. */
  def add(a: Int, b: Int): Int = a + b
}
